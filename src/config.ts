// Esta função gera o link que será exibido no seu "Preview Modal"
export const getInviteUrl = (id: string) => {
  // Por enquanto, usamos um link de exemplo. 
  // Futuramente, aqui será o link real do seu site de convites.
  return `https://convitesconexoesmagia.com.br/invite/${id}`;
};

export const SITE_NAME = "Convites Conexões Magia";